# 项目工作规则

本仓库做的是“描述物品 → AI 识别结构 → 机制轴编译武器 → 打怪过关”的 Godot 本地原型，不是仅做枪械，也不是正式线上产品。

## 开工先读

- 先读本文件、[GAME_DESIGN.md](GAME_DESIGN.md)、[PROJECT_STATUS.md](PROJECT_STATUS.md)、[当前交接](docs/NEXT_PHASE_HANDOFF.md)，再读涉及模块的专项文档。
- 核对实际目录、分支、HEAD 和 `git status`；状态文档是有日期的快照，不能替代现场检查。
- 保留已有未提交改动，不回滚他人工作。当前工作目录应是本仓库，不是上层 `playlab`。
- `docs/` 中旧阶段报告是历史证据，不能用其中旧限制覆盖后来明确确认的设计。

## 设计底线

- 玩家负责描述“是什么”，不负责选择怎么握、怎么打或填写机制参数；可确认视觉像不像，纯草图可询问物品身份。
- AI 返回结构与机制声明，本地代码校验、编译和执行；不运行 AI 生成的游戏代码。
- 不用具体物品名、枪名、敌人名或样本 ID 硬编码招式。身份路由、别名和美术索引不等于战斗配方。
- 机制轴必须影响实际动作、有效接触、代价或敌人反应，不能只改卡片文字；有条件生效的轴要声明条件。
- 用真实 Alpha、握点和结构绑定校验能测到的几何；不能从归一化图片伪造现实尺寸、材质或重量证据。
- 物品像素、握持、结构变化与命中数据保持一致；调试线不能冒充成品鱼线、伞面等结构。
- 解析或图像校验失败要明确停止，不偷偷换成默认枪、无关缓存或伪造机制补成功。
- 枪械是通用武器的一条路径，改普通物品时保留枪械回归；敌人由系统提供，不要求玩家输入敌人。

## 技术与运行

- Godot 4.7.1、GDScript；桥接为 Python，启动与测试脚本为 PowerShell。像素纹理使用 nearest filtering。
- 当前主入口：`scenes/sunny_expedition.tscn`，启动器 `Play-Sunny.cmd` / `scripts/run_sunny_expedition.ps1`。晴日造物、武器库、一张向右推进的长关卡和结算共用此入口；关卡内四个路标依次开放，清完前三段后各做一次局内机制三选一，选完才放行，第四段进入守门战。强化必须来自机制轴/枪械运行参数并真实改变战斗，不能按物品名写增益。`Play-Church.cmd` 是保留的旧教堂版，Sunny/Church 分开存档；旧 `open_identity_spike` 仅作兼容与草图试验，不是最新试玩。
- 统一离线回归：`./scripts/test_complete_weapon_system.ps1`，使用隔离武器库，不读取 `.env`。
- 旧 `scripts/test.ps1` 不是当前总验收入口；运行旧脚本前检查其存档与环境副作用。
- 在线生成启动器可能收费；遵守用户已批准的调用范围。文档整理、离线测试不顺带调用在线 AI。
- 不把密钥、`.env` 内容或个人凭证写入报告、日志、存档或提交。

## 验证与协作

- 分开报告：静态检查、离线测试、机器人输入、真实图形回放、桌面手动试玩。未执行的不得写成已通过。
- 证据注明日期、场景、数据来自缓存/模拟/实时 AI；进程退出成功不等于玩法成功。
- 美术改动需要独立视觉复核，查看真实游戏截图；说明审核者和采样范围，不把代理审核称作外聘画师认证。
- 清晰像素、人物/武器比例、手臂连接、结构可读性和攻击预警优先。截图通过不等于全动画或整体平衡通过。
- 工具权限不足如实记录，不绕过拒绝；代码/终端权限不等于桌面应用操作权限。
- 只有明确授权并适合独立工作的任务才并行委派；先定基线与文件责任，主控整合后验证，不直接照抄子代理的完成声明。
- 讨论、解释、状态、只读审计不自动授权代码修改。开发请求按已确认范围推进，重大设计选择或新增权限才向用户确认。
- 不把“继续做”扩大成任意付费、购买素材、改权限、删除存档/worktree、提交推送或合并其他仓库的授权。

## 收尾与交接

- 规则放本文件；已确认设计放 `GAME_DESIGN.md`；实际状态、证据、缺口和下一步放 `PROJECT_STATUS.md`。
- 改动后更新相关文档；把提议、实现、验收分开，不宣称全部机制轴正交或全部物品可用而无对应证据。
- 新分支/worktree 只继承提交，不自动继承当前未提交文件；先明确保存或转移方式，不丢进度。
- 不因整理文档删除旧报告、测试截图、缓存、存档或其他 worktree；不手工修改自动生成的代理记忆。
- 获得提交/推送授权后，先按 docs/ASSET_UPLOAD_SCOPE.md 审查实际暂存区；不把忽略规则误当成已移除被跟踪的私人文件。开发分支不自动合并 main。
