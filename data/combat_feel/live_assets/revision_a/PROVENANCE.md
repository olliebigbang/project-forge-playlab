# Combat Feel Revision A frozen Live asset provenance

`giant_wooden_spoon` is a byte-for-byte copy of the accepted Live E2E L03 final handoff in:

`tools/live_e2e/output/runs/spike7-20260803t234041483z-82d0a3b3/L03/`

The copied final handoff contains the real FLUX/BiRefNet 96×96 sprite, the frozen
`forge-semantic-v1.1` blueprint, player-confirmed anchors, and the Live result
manifest. Source evidence is never moved or overwritten. The SHA-256 values in
`index.json` pin every copied file.

As of Revision A implementation, no frozen real Open Playtest output for the
frying pan or old mop exists in this repository or the Playlab Godot user-data
directory. Their developer fixtures remain diagnostic-only and are not listed as
Live assets. They must be generated and identity/anchor-confirmed through Open
Playtest before they can be added to this index.
